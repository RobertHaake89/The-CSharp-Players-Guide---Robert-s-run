using System;
using RoomCoordinates;

namespace RoomCorrdinates;

class Methods
{
    public static bool MatchCoords(Coordinate one, Coordinate two)
    {
        bool isEqual = true;

        if (one._row == two)
    }
}